# gpp_utils 開発方針

## 実装時の並列化

独立した実装・検証・文書作業は、利用可能な場合はサブエージェントへ積極的に委譲する。担当範囲、依存する公開契約、受入条件を委譲時に明記し、同じファイルを複数エージェントが同時に編集しない。親エージェントが全体設計、共有API、依存関係の調整、最終統合と受入判定を担当する。

親エージェントを含めて最大4エージェント（親+3）を同時に稼働させる。独立作業がない場合は無理に分割しない。計算・保存など重要な処理は、実装担当とは別のエージェントによるレビューを行う。

## モデル選択とコスト管理

タスクの複雑さに応じ、必要十分な下位モデルを優先して使用する。GPT-5.6 Lunaは機械的な整理、参照更新、設定例、仕様が確定した文書修正に割り当てる。GPT-5.6 Terraは境界が明確な通常実装、設定検証、CLI、TSV、局所的テストに割り当てる。GPT-5.6 Solはソルバー、差分評価、乱数、保存・再開、複数モジュールにまたがる実装に割り当てる。モデルIDはそれぞれ`gpt-5.6-luna`、`gpt-5.6-terra`、`gpt-5.6-sol`とする。

仕様の不確実性、同じ失敗の反復、変更範囲の拡大が生じた場合は、親エージェントまたはより高い適合度のモデルへ切り替える。利用できないモデルは利用可能なモデルで代替し、作業を停止しない。モデル選択による費用削減額は計測していない限り主張しない。

## 変更と検証

各エージェントは担当範囲外のファイルを変更しない。共有契約を変更する場合は、親エージェントが先に契約を確定し、影響する担当へ通知する。各担当は変更内容、実行した検証、未解決事項を親へ報告する。親は統合後に`python scripts/check.py`、文書・スキーマ整合性を確認する。このスクリプトはfmt、locked clippy、全ターゲット／docテスト、release exact regression、release build、警告をエラーにするdoc生成を同じ順序で実行する。個別コマンドをCIと別に増やさない。

本ファイルと`docs/application-plan.md`は実装時のエージェント分担・モデル選択の正本である。計算仕様は`docs/algorithms.md`、保存形式とTSV列は`docs/output-format.md`、拡張時の互換性確認は`docs/extending.md`を正本とする。過去の実測は`docs/verification.md`にあり、現在のCI成功を表すものではない。

## 実験のベースライン

今後の計算は、2026-09-25に定めたSA/EO×Flip/Swapのベースラインを基準とする。平滑化や適応度の比較など新しい実験は、グラフ・ステップ数・実行シード・計測条件をベースラインにそろえ、ベースラインの結果と比較できるようにする。そろえた条件は条件IDが一致し、`gpp`が既存の結果を再利用する。ベースラインから外れる条件は利用者が指示した場合だけ使い、外れる点を仕様ファイルと報告に明記する。

- グラフ：Random・Geometric×頂点数124・250・500×平均次数5・10・20の18組。各組インスタンス1つ（グラフシード0）。
- 探索：10^6ステップ、実行シード0〜31の32個、alpha=0.05。
- SA：Θ=-1.50〜+2.50の0.05刻み81点、温度T=10^Θ、平滑化なし。
- EO：τ=0.00〜3.00の0.05刻み61点、適応度`default`。
- 計測：対数間隔の56点。現行解と暫定解から実目的関数で山登りしたベイスン値を記録する（`max_basin_steps=10000`）。

条件の正本は`experiments_sa_eo/make_baseline_v1.py`とし、生成した`experiments_sa_eo/baseline_v1.toml`（284条件/グラフ、163,584ジョブ）をそのまま`gpp`へ渡す。温度などの数値は同じ式で生成し、手で書き写さない。浮動小数点の末尾が変わると条件IDが変わり、既存の結果を再利用できなくなる。再定義前の旧ベースライン（10^7ステップ、4インスタンス）と旧データ（`data/legacy_archive/`）は比較用の参照にとどめ、新しい結果とrun単位で混ぜない。

ベースラインにそろえた復旧runの計画（実行順A→C→B、未実行）は`experiments_sa_eo/RECOVERY_PLAN_v1.md`にある。実行は利用者の指示を受けてから`python experiments_sa_eo/run_recovery_v1.py`で行う。
