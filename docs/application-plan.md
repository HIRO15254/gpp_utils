# gpp_utils 完成版アプリケーション仕様・実装計画

状態: 完成版の機能を実装済み。Windowsでの検証結果と残る環境確認は[verification.md](verification.md)に記録する。Linux向けCIは追加済みだが、この環境でのLinux実行確認は未完了。保存形式の詳細は[output-format.md](output-format.md)を参照。

計算結果を維持する高速化の比較契約・実装方針・測定結果は[performance.md](performance.md)を参照。比較基準は `51577f9` とし、実行時間と試行IDを除く計算結果をビット単位で維持する。

## 1. 目的と対象範囲

完成版は、グラフ分割問題について、条件を定義し、複数手法・パラメータ・シードで実験し、中断や失敗から再実行し、結果をJSON・TSVとして取り出せるローカルCLIアプリケーションとRustライブラリとする。

今回、従来保留していたTOML設定、パラメータスイープ、HC・EOのバッチ対応、探索と計測の乱数分離、実行中キャンセル、未完了ジョブの再実行を完成させる。GUIとSQAは本体・公開API・依存・テスト・使用例・現行の説明から削除する。

ユーザーが確定した方針:

- GUIとSQAは不要。
- 全手法でFlipと等分割Swapを選択できるようにする。EOのtausは必須。
- EOの組み込み適応度は`default`一つで、定義は`good_edge_fraction`。Rustで定義を追加・登録し、設定から選択する拡張性は維持する。
- 平滑化はHC・SAのみ。`k_average`は削除する。
- 旧Rust API、CLI、設定、保存形式との互換性は不要。既存データ自体は削除しない。
- 完成形の構成と実際に使える機能をこの文書にまとめ、実装時も更新する。

追加回答で確定した方針と、本計画の実装範囲:

- 連続緩和は対象外とすることが確認済み。現状は未完成ランナーからの呼び出しのみで、連続目的関数・離散化の実装がない。完成版の設定やコマンドにも未実装の選択肢を残さない。
- JSON・TSV出力のみとし、集計・作図を外部で行うことが確認済み。シード平均、統計比較、gnuplot、グラフ描画、Web画面は含めない。実験の状態や各実行の最良値を一覧にする機能は含める。
- 再開はジョブ単位。完了済みジョブを再利用し、未完了ジョブを同じ条件・シードで最初から実行する。探索の途中状態やRNG状態を復元するチェックポイント再開は実装しない。
- 単一マシンでのCPU並列実行を対象とし、分散実行・サーバー・クラウド連携は含めない。

## 2. 完成版で解く問題とアルゴリズム

### 2.1 問題

無向・重みなし・自己ループと多重辺のないグラフの頂点を2群へ分け、次を最小化する。

`score = cut_edges + alpha * (size_a - size_b)^2`

`alpha` の既定値は0.05とし、設定可能にする。Flipでは任意の分割を探索し、空の群も許容する。Swapでは偶数頂点を必須とし、初期状態から両群をn/2頂点に保つためペナルティは0となる。各計測点の現行解と暫定解は共有分割プールへの`SolutionId`参照で保存し、合計スコア・カット数・群サイズ・ペナルティは読み込み時に算出する。

Flipは1頂点反転で近傍数n、Swapは異なる群の1頂点ずつの交換で近傍数n²/4。Swapの中間状態を計測・最良解更新へ露出しない。奇数頂点とSwapの組み合わせは設定検証で拒否する。

入力グラフは、既存のRandom（Erdős–Rényi）とGeometric（単位正方形上の幾何グラフ）の生成をサポートする。グラフ生成シードと探索シードは独立に指定する。Geometricの距離閾値は現行式を維持し、指定次数が境界効果を補正しない生成パラメータであることを明記する。実際の平均次数は保存済み辺リストから算出して表示する。任意形式の外部グラフインポートは今回追加しない。

### 2.2 対応する探索

| 設定名 | 動作 | パラメータ | 1ステップ |
|---|---|---|---|
| `hc` | 選択したFlip/Swapの全近傍から最良の改善を選択 | 平滑化 | 全近傍走査1回。改善なしで終わる最後の走査も数える |
| `sa` | Flipは頂点、Swapは両群から各1頂点を一様選択し、Metropolis規則で判定 | `temperature >= 0`、平滑化 | 受理・棄却判定1回 |
| `eo` | 頂点適応度を順位付けし、順位rを重みr^(-tau)で選択 | 必須`tau > 0`、適応度 | 反転または交換1回。常に受理 |

平滑化SAは独立した重複実装を作らず、`sa`と平滑化の組み合わせとして扱う。温度スケジュールは導入せず、固定温度であることをヘルプにも表示する。SAの温度0では厳密な改善のみ受理し、正温度では同点も受理する。

EOの組み込み登録名は`default`、定義は`good_edge_fraction`とする。頂点iの同群接続数g_i、異群接続数b_iから`g_i/(g_i+b_i)`を求め、孤立頂点は1とする。`good_edge_fraction`は定義説明に使用し、設定名の別名にはしない。条件に登録名と定義バージョンを残し、説明文字列を各結果に複製しない。`good_edges`・`negative_bad_edges`・候補評価差は組み込まない。この定義は[原論文Sec. III.B](https://arxiv.org/pdf/cond-mat/0104214)に基づく。

適応度の小さい頂点を順位1とし、同点群は専用RNGで一様に並べ替える。Flipでは全体の順位分布から1頂点を選択する。Swapでは1頂点目を全体から選び、全体順位の重みを保ったまま反対群へ条件付けした分布から2頂点目を選ぶ。反対群内で順位を振り直さず、無制限な再抽選も行わない。HCの同点改善候補は専用乱数で一様選択する。

tausは非空の有限・正の値の配列を必須とし、自動推定を廃止する。Rust APIの単一tauも必須にし、OptionやDefaultで補完しない。`eo + swap + default`を論文に沿う基本構成、`eo + flip`を同じ適応度を反転へ適用する変種と明示する。適応度へバランスペナルティは暗黙に加えない。

`VertexFitness`は現在の状態から全頂点の有限な適応度を返し、小さいほど低適応度とする。同じ状態・パラメータには同じ値を返し、探索状態や順位選択用RNGを変更しない。`FitnessRegistry`で定義の検証・生成処理を登録し、名前・バージョン・正規化済みparamsを解決する。追加定義はRustで実装・登録して再ビルドする。動的ロードや数式評価器は追加しない。キャッシュはジョブが所有し、独自定義に局所更新を要求しない。差し替え例・テスト用定義は配布CLIへ登録しない。

全手法に最大ステップ数を設定し、HCにも上限を適用する。最良解は実スコアが改善したときのみ更新し、同点では先に見つけた解を保持する。「最終解」と「訪問した解のうち実スコアが最良の解」を両方返す。棄却したSA候補や計測用山登りの解は探索の最良解に含めない。

### 2.3 平滑化

次の4種類をHC・SAから指定できる。`k_average`とその公開型・設定・使用例は削除する。EOへのsmoothing指定はnoneを含めてエラーにする。

| 設定名 | 定義・Kの扱い |
|---|---|
| `none` | 元の目的関数 |
| `all_average` | 選択した近傍の全距離1近傍の平均 |
| `random_k_average` | K個の非復元抽出。距離1近傍数Mを超えれば全距離1近傍と必要数の距離2近傍を使用。Kは1以上、上限は距離1と距離2の合計数 |
| `weighted_average` | `w * 全距離1近傍平均 + (1-w) * 元スコア`。`w=min(K,M)/M`、K=0も許可 |

Flipの距離2近傍は異なる2頂点の反転で個数C(n,2)。Swapは各群から2頂点ずつを選んだ交換で個数C(n/2,2)²。元状態・距離1の状態・重複は含めない。頂点番号順の正規な近傍番号を用い、候補状態の全実体化を避ける。

Kの正規化はグラフごとに行い、計画表示で要求値と有効値を示す。正規化後に同じジョブになる重複設定はエラーとし、無断で回数を減らさない。

探索中のランダム平滑化は呼び出しごとに再サンプリングする。SAは受理時の評価値を次の判定まで保持する。HCは現状態と候補を走査時に評価し、選択された候補の評価値を引き継ぐ。確率的評価のHCの改善なし終了は厳密な局所最適性を保証せず、終了理由を`no_sampled_improvement`とする。

## 3. ユーザーの操作とCLI

Cargoパッケージとライブラリ名は`gpp_utils`、配布する実行ファイルは`gpp`とする。旧`cli`・`gui`・`experiment`バイナリは廃止し、入口を一つにする。

| コマンド | 実際にできること |
|---|---|
| `gpp init experiment.toml` | コメント付きの小規模設定例を生成。`.json`なら同等のJSONを生成。既存ファイルは上書きしない |
| `gpp validate experiment.toml` | 構文、手法固有のパラメータ、全組み合わせ、重複、数値範囲を検証。グラフ生成・実験はしない |
| `gpp plan experiment.toml` | 展開した条件と総ジョブ数を表示。`--out experiment.json`指定時だけ、既定値・バージョンを固定した未展開の条件を保存。グラフ生成・実験はしない |
| `gpp run experiment.toml --root data/v1 --threads 4` | 検証、未展開の条件保存、グラフ準備、並列実行、結果保存を行う |
| `gpp run --experiment experiment.json --root data/v1 --threads 4` | 保存したバージョン付き条件を展開して実行。設定ファイル入力との同時指定は不可 |
| `gpp resume --batch <batch_id> --root data/v1 --threads 4` | 保存条件を再展開し、検証済み完了結果を再利用して残りを最初から実行 |
| `gpp inspect --batch <batch_id> --root data/v1` | 計画、状態別件数、各ジョブの実スコア最良値、終了理由、エラーを一覧表示 |
| `gpp inspect --batch <batch_id> --condition <condition_id> --seed <seed>` | 単一実行の条件、分割から算出した最終・最良指標、計測状態を表示 |
| `gpp export --batch <batch_id> --root data/v1 --out export` | 条件と結果からTSVを生成。`--condition <condition_id> --seed <seed>`で単一実行に限定可 |

`run`と`resume`は既定で利用可能な論理CPU数をワーカー数とする。`--threads`は1以上とし、実験の科学的な条件や識別子には含めない。`--root`の既定値は現在の作業ディレクトリを基準とする`data/v1`。設定内に出力先やスレッド数を重複して持たせない。

`run --overwrite`は選択された計画のジョブだけを再計算する。再計算失敗時に既存の完了結果は失わない。指定なしでは、検証済みの同一結果があれば再利用する。全出力先を消去する操作は用意しない。

`validate`・`plan`・`inspect`・`run`・`resume`の`--json`で機械可読な最終結果を標準出力へ出せるようにする。進捗と診断は標準エラー、最終結果は標準出力に分け、ライブラリ自体はprintしない。

終了コードは0=成功、1=実行・保存・出力などの失敗、2=引数・設定エラー、130=ユーザー中断とする。スキップのみの実行も成功。バッチ内に失敗ジョブがある場合は他ジョブを継続し、最終終了コードを1とする。中断と失敗が混在する場合は130を返し、失敗件数は標準出力の最終報告に含める。

典型的な操作:

```text
cargo build --release --bin gpp
gpp init experiment.toml
gpp validate experiment.toml
gpp plan experiment.toml
gpp run experiment.toml --threads 4
gpp inspect --batch <runコマンドが表示したbatch_id>
gpp resume --batch <batch_id> --threads 4
gpp export --batch <batch_id> --out export
```

## 4. 設定とスイープ

TOMLとJSONは同じ`ExperimentSpec`へ読み込む。形式による動作差はなく、拡張子で選択する。未知のフィールドと非対応の手法をエラーにし、誤記を無視しない。

設定は次の構成とする。

| 項目 | 意味・既定値 |
|---|---|
| `schema_version` | 必須、初版は1 |
| `name` | 任意の表示名。省略時は入力ファイル名。識別子には含めない |
| `run_seeds` | 必須、空でないu64の配列。探索を繰り返すシード |
| `neighborhoods` | 必須、flip・swapから選ぶ非空・重複なしの配列 |
| `problem.alpha` | 有限かつ0以上。既定0.05 |
| `budget.max_steps` | 必須、1以上の整数。暗黙の切り詰めや10のべきへの制約なし |
| `measurement.schedule` | `logarithmic`（既定）または`explicit` |
| `measurement.steps` | `explicit`のときのみ指定する一意なステップ配列。0と終了時点は自動追加 |
| `measurement.basin` | `none`、`real`、`both`。既定`both` |
| `measurement.max_basin_steps` | 各ベイスン山登りの最大走査数。1以上、既定10,000 |
| `measurement.diagnostics` | 既定false。trueの場合のみ最終累積カウンター・時間内訳とベイスン走査数を保存 |
| `graphs[]` | `kind`、`node_counts[]`、`expected_degrees[]`、`seeds[]`。各配列は必須・非空 |
| `solvers[]` | `kind`と、その手法で有効なパラメータ配列、`smoothing[]` |

SAのtemperaturesとEOのtausは必須。HCに温度・tauは指定できない。HC・SAのsmoothing省略時はnone一つ。Kを必要とする種類のみ非空のks配列を受け付ける。EOはfitnesses配列を受け付け、省略時は`{ kind = "default" }`一つへ解決する。指定する場合は非空とし、定義固有のparamsは各項目で明示する。組み込みdefaultのparamsは空のみ許可する。異なるparamsの比較は項目の列挙で行い、未知の定義は拒否する。

配列による明示列挙を唯一のスイープ構文とする。式評価は導入しない。各定義の直積を展開し、「グラフ×近傍方式×ソルバー設定×探索シード」を実行する。EOはtaus×fitnesses、HC・SAは手法パラメータ×smoothingを展開する。表示順は入力順、乱数と識別子は配列位置に依存させない。

次の例は1グラフ、2近傍方式、SA 6設定・HC 1設定・EO 2設定、探索3シードで合計54ジョブとなる。

```toml
schema_version = 1
name = "comparison"
run_seeds = [0, 1, 2]
neighborhoods = ["flip", "swap"]

[problem]
alpha = 0.05

[budget]
max_steps = 100

[measurement]
schedule = "logarithmic"
basin = "both"
max_basin_steps = 100
diagnostics = false

[[graphs]]
kind = "random"
node_counts = [12]
expected_degrees = [5.0]
seeds = [42]

[[solvers]]
kind = "sa"
temperatures = [0.1, 1.0]
[[solvers.smoothing]]
kind = "none"
[[solvers.smoothing]]
kind = "random_k_average"
ks = [8, 32]

[[solvers]]
kind = "hc"
[[solvers.smoothing]]
kind = "none"

[[solvers]]
kind = "eo"
taus = [1.2, 1.5]
[[solvers.fitnesses]]
kind = "default"
```

検証は展開後の全条件にも適用する。頂点数は2以上、次数は0以上n-1以下、各浮動小数点値と目的関数の上限は有限、シードと件数の算術はchecked演算とする。重複シード、同一グラフ、同一有効実行設定を拒否する。指数変換の旧設定は廃止し、温度とステップ数を実値で受け取る。実行中に非有限の評価値が発生した場合も、NaNの順位付けや保存を続けず、そのジョブを原因付きの数値エラーとして終了する。

## 5. 実行・計測・再現性

### 5.1 共通実行経路

CLIとライブラリは同じ設定検証、計画展開、単一実験、計測を呼び出す。ソルバーは探索状態を更新し、実験側が計測時点と停止を管理し、保存側が結果を確定する。

グラフごとに一つの読み取り専用Problemを共有し、探索状態・平滑化器・乱数・計測器はジョブが所有する。ジョブ間で可変RNGを共有しない。全近傍の解ベクトルを毎回作る経路を避け、整数状態と差分評価を利用する。HC・EOも同じ差分評価を利用する。

### 5.2 乱数

再現性の契約は「同じアルゴリズムバージョン・グラフ・条件・探索シードなら、並列数、実行順、表示名、計測頻度、計測有無にかかわらず同じ探索結果」である。実行時間とイベント到着順は一致対象から除く。

- 初期分割はグラフ内容・近傍方式・探索シードから導出した専用RNGで作り、同じ近傍方式では手法・パラメータを変えても同じ初期分割にする。Swapは半数ずつの所属を一様にシャッフルする。
- 探索の候補選択・受理、ランダム平滑化、探索の同点選択を別ストリームにする。導出元はグラフ内容、近傍方式、有効な手法・平滑化または適応度の条件とバージョン、探索シード、アルゴリズムバージョン。予算と計測条件は含めない。
- 計測のRNGは探索から独立させ、各チェックポイントのステップ番号と用途から個別に導出する。他の計測点を追加しても共通の計測点の値が変わらないようにする。
- 導出は固定のSHA-256と用途ラベルを使い、固定エンディアンで64bitシードへ変換する。ハッシュ・変換規則を仕様とテストで固定する。RNGは既存のMT19937-64を継続する。

この乱数設計は既存の一部の結果を意図的に変える。構造整理段階の旧80ケースの一致確認と、新仕様の参照実装との一致確認を別の検証として扱う。

### 5.3 計測

初期状態0、対数刻みまたは明示されたステップ、終了時点を記録する。同じステップを重複保存しない。HCの早期終了後の値を、存在しない後続ステップへ埋めない。

各計測点には、ステップ終了時の現行解と、そのステップまでに実評価値が最小だった暫定解を保存する。これらは重複排除した`partitions`配列への`SolutionId`参照であり、同一の分割を複数回保存しない。暫定解は計測のないステップでも更新し、同点では最初に得た分割を維持する。初期点と終了点は必ず記録し、終了点の現行解・暫定解はトップレベルの参照と一致させる。各時点の実評価値、カット数、群サイズ、ペナルティは分割から読み込み時に算出する。

現行解は探索状態が保持する解だけを対象とし、SAで棄却した候補やベイスン計測中の解を暫定解へ混ぜない。計測間に改善後再び悪化した場合も、計測点の現行解の最小値で暫定解を代用しない。`SolutionId`の範囲、分割長、頂点順を読み込み時に検証する。

HC・SAの非自明な平滑化では現在の平滑化評価を保存し、ランダム平滑化だけは探索が保持する評価値も保存する。決定的平滑化では両者が同じなので複製しない。noneとweighted_averageの有効K=0は実評価から表示値を生成する。EOの平滑化関連列は非適用とする。

ランダム平滑化の計測では、チェックポイントごとに近傍番号の抽出集合を固定する。同じ番号集合を現在解・候補・ベイスン終点の評価に使う。Swapでは現在の各群を頂点番号順に並べた正規の近傍列に番号を対応させ、固定した評価関数として常に有効な交換を評価する。「探索が保持する確率的評価値」と「計測用の固定集合による現在評価」は別の測定値である。

ベイスン計測は探索と同じFlip/Swap近傍で行う。同点改善は計測専用RNGで一様選択し、探索へのフィードバックは行わない。実空間・平滑化空間からの終点を必要な空間で評価して保存する。最大走査数で打ち切り、local_optimumまたはstep_limitを保存し、打ち切り終点を局所最適解と表示しない。ベイスン終点の分割を保存しないため、測定したスコアは必要な一次データとして残す。

noneとweighted_averageの有効K=0ではbothでも実空間を1回だけ計測し、対応する平滑化指標は読み込み時に補完する。EOのbothは実空間のみと解釈し、平滑化側は補完しない。有効な計測モードは条件から求め、別フィールドとして保存しない。非適用・未実施の値はJSONではキーを省略し、TSVでは空欄とする。NaNは保存しない。

measurement.diagnosticsは既定false。trueの場合だけ、最終累積値として適用移動数、探索・計測それぞれの目的関数実計算数、EOの頂点適応度実計算数、探索・計測の時間内訳を保存し、ベイスンごとの走査数も追加する。キャッシュ参照は実計算数に含めない。受理数・棄却数・受理率は既存値から生成する。診断の有無は探索の乱数と結果を変えない。通常保存する時間は単一実験全体のelapsed_msだけとし、グラフ生成・ディスク保存を除く。詳細な項目と省略規則は[出力仕様](output-format.md)に従う。

### 5.4 中断と再実行

Ctrl+Cの1回目で新規ジョブの開始を止め、実行中ジョブに協調的な中断を要求する。探索ステップだけでなく、近傍走査・平滑化の大量評価・ベイスン探索・グラフ生成のループも最大1,024要素ごとに確認する。ファイルの書き込み中は確定または失敗まで待つ。2回目のCtrl+Cは終了コード130で即時終了し、書きかけファイルは完了結果として扱わない。

中断時は可能なら最後の確定した現行解・暫定解と既存計測を最新の未完了マーカー内の部分結果として保存する。未開始ジョブの状態ファイルは作らず、条件とファイルの有無から判断する。最後の計測が未完了なら測定値を省略し、中断処理のために高コストなベイスン計測を新たに始めない。

`resume`は完全な結果と設定の一致を検証して再利用し、残りを先頭から実行する。中断地点からの継続ではないことをヘルプと進捗に表示する。最新の未完了試行だけを保持し、再試行時に置き換える。成功履歴や過去試行の全履歴は保存しない。

## 6. 保存・識別子・結果出力

### 6.1 保存構成と一次データ

```text
data/v1/
├── .writer.lock
├── graphs/<graph_id>.json
├── batches/<batch_id>/experiment.json
└── runs/<condition_id>/
    ├── seed_<seed>.json
    └── seed_<seed>.incomplete.json  # 最新の未完了試行がある場合のみ

export/                            # 明示的なexport時だけ作成
├── runs.tsv
├── traces.tsv
└── metadata.json
```

条件・グラフ・解・集計から復元できない測定値を保存する。展開済みplan.json、manifest.json、summary.json、状態一覧、集計表を自動保存しない。件数・進捗・結果パスは保存条件を展開してファイルを検証することで求める。

experiment.jsonはschema_version、versions、specを持つ。specは既定値を明示した未展開のExperimentSpecで、配列によるスイープ条件をそのまま持つ。versionsでアルゴリズム、RNG導出、グラフ生成、計測、設定展開・正規化、使用する適応度定義の版を固定する。入力全文と展開済み設定を二重保存せず、有効Kや実効計測モードも読み込み時に求める。planの--outとバッチ保存で同じ形式を使う。

グラフJSONはschema_version、node_count、edges、content_hashのみ。edgesはu<vの無向辺ペアを辞書順に並べる。隣接リスト・辺数・次数・平均次数は読み込み時に算出する。生成条件はexperiment.jsonに置き、幾何座標は保存しない。ロード時に頂点範囲、辺の順序・重複・自己ループ、内容ハッシュを検証する。未作成のみ自動生成し、破損や権限エラーは関連ジョブの失敗とする。破損グラフを無断で再生成して置き換えない。

### 6.2 識別子とバージョン

- graph_id: 正規化した生成条件と生成器バージョンのハッシュ。
- condition_id: 単一のグラフ生成条件、alpha、近傍方式、有効なソルバー・平滑化または適応度、予算、計測・診断設定、関連バージョンのハッシュ。探索シードは含めない。
- 実行の識別: (condition_id, seed)の組。パスに一度だけ置き、結果本文に複製しない。同じ組はバッチ間で再利用する。
- batch_id: バージョン付き実験条件を正規化し、条件・シードの順序に依存しない形でハッシュ化。表示名、出力先、スレッド数を含めない。

全ハッシュはSHA-256の全長を使い、画面だけ短縮する。固定順の構造化バイト列、浮動小数点のIEEE 754ビット表現、負のゼロの正のゼロへの正規化を仕様化する。表示桁数で丸めた値をキーにしない。識別子はグラフ生成前に決まり、planはグラフ生成を必要としない。

グラフ内容ハッシュはグラフファイルの破損検出に使い、同じgraph_idの内容は不変とする。探索の乱数導出には実グラフ内容を使うが、trajectory_id、job_keyやそれらの対応表を保存しない。条件とバージョンを再展開して結果パスを解決し、未対応バージョンを明確に拒否する。

### 6.3 完了判定と最小の試行管理

同じ保存ルートへの書き込みはOSが解放する排他ファイルロックで一つに制限する。競合したrun・resumeはエラーを返す。書き込みは同一ディレクトリの一時ファイル、flushと同期、原子的な確定処理を用い、WindowsとLinuxで既存結果を失わないことを検証する。inspect・exportは確定済みファイルを読み取り専用で参照する。

開始時にschema_version、attempt_id、status=runningだけの未完了マーカーを作る。失敗・中断時は最新の状態、必要最小限のエラー、保存可能な部分結果へ更新する。正常完了時は同じattempt_idを持つseed_<seed>.jsonを確定し、マーカーを削除する。保存成功後のみJobCompletedを発行する。

結果確定直後に停止してマーカーが残っても、同じattempt_idの有効な結果を優先して清掃する。排他ロック取得後にrunningマーカーだけが残る場合は中断と判断する。以前の完了結果と異なるattempt_idの失敗マーカーがある場合は上書き再実行の失敗として区別し、旧結果を保持する。通常のresumeはその旧結果を再利用する。

結果の破損は通常のrunではエラー。resumeまたは--overwriteでは破損ファイルを調査用に退避して再計算する。この例外以外では過去試行を蓄積しない。非対応バージョンは破損と区別し、自動変換しない。未開始件数や完了一覧は条件とファイルから算出し、別の正本を管理しない。

### 6.4 結果とTSV

保存フィールド、型、具体例、TSV列順、省略規則は[output-format.md](output-format.md)を正とする。

完了結果はschema_version、attempt_id、termination、completed_steps、elapsed_ms、重複排除したpartitions、final_solution、best_solution、best_step、records、任意のdiagnosticsを持つ。final_solutionとbest_solutionはpartitionsのSolutionIdを参照し、同一分割は一度だけ保存する。最終・最良スコアと内訳、条件・シード・バージョンの本文複製は保存しない。初期分割は初期レコードから参照でき、保存された分割から各計測点の実評価値を復元する。

recordsは第5.3節の観測値を保存し、各点のstepとcurrent_solution/best_solutionのSolutionId、および集計から復元できない平滑化・ベイスン測定値だけを持つ。実評価値と内訳は参照分割から算出する。表示・export用のRunViewが参照を解決し、グラフと分割から派生値を算出する。

runs.tsvは各ジョブ1行で条件、状態、最終・最良指標などを展開する。未開始・失敗ジョブも行を持ち、結果のない列は空欄とする。traces.tsvは完了ジョブの各計測点1行で、補完した現在・最良値、平滑化・ベイスン指標を出す。途中の分割・内訳・時間・累積カウンターは保存していないため出力しない。--include-incomplete指定時だけ、完了結果がないジョブの最新部分結果を追加し、異なる試行を混ぜない。

metadata.jsonは出力形式、時刻、対象バッチ、列の説明、部分結果を含むかのみ。単一実行への絞り込みがある場合はその条件IDとシードも残す。条件全文、全ジョブ一覧、件数集計は複製しない。件数・除外理由の要約は標準出力へ表示する。TSVとmetadataは通常実行では作成せず、保存データから実験を再実行せずに再生成できる。

列順と表現を固定し、condition_id・seed・step順に出力する。タブ・改行・引用符は引用処理し、非適用値は空欄とする。出力先の既存ファイルは--overwriteなしで上書きしない。破損結果を黙って除外せず、exportを非0で終了する。

## 7. リポジトリと内部アーキテクチャ

```text
gpp_utils/
├── AGENTS.md                      # 並列分担・モデル選択・検証方針
├── Cargo.toml / Cargo.lock
├── README.md
├── src/
│   ├── lib.rs / error.rs
│   ├── optimization/              # CancellationToken・用途別RNG
│   ├── graph_partition/           # graph.rs生成・state.rs差分更新
│   ├── smoothing/                 # 全平均・加重平均・距離1/2抽出
│   ├── fitness/                   # VertexFitness・FitnessFactory・登録
│   ├── solvers/                   # 共通EngineによるHC・SA・EO
│   ├── experiment/                # config・plan・runner・measurement・result
│   ├── storage/                   # グラフ・条件・バッチ・結果とatomic
│   ├── export/                    # TSV・列メタデータ
│   └── bin/gpp/main.rs            # 全サブコマンド
├── examples/
│   ├── configs/                   # minimal・54ジョブcomparison
│   ├── basic_usage.rs
│   └── bench_search.rs
├── tests/                         # CLI・実験受入・独立参照計算
├── docs/
│   ├── application-plan.md
│   ├── output-format.md
│   ├── algorithms.md
│   └── verification.md
└── .github/workflows/ci.yml
```

前案のようにソルバー本体と実験専用SAを別々に増やさず、移動・評価の高速なバックエンドを共通のソルバーから呼ぶ。汎用の全再計算経路は小規模テストの参照として使い、本番で同じアルゴリズムの制御ループを二重管理しない。

`PartitionState`は分割、総カット数、各群サイズ、頂点別カット数を所有し、外部から個別に書き換えられない。スコア計算と更新を同じ実装へ集約する。グラフは読み取り専用とし、解状態への変更でグラフを複製しない。

ライブラリの中心となる公開インターフェース:

- `ExperimentSpec`: 入力形式に対応する未検証の設定。
- `StoredExperiment`: 未展開の正規化済み設定と固定バージョン。experiment.jsonへ保存する唯一の条件型。
- `compile_experiment(spec) -> Result<ExperimentPlan>`: 検証・正規化・メモリ上での展開。I/Oとグラフ生成を伴わず、展開済みPlan自体は保存しない。保存条件の再展開では固定バージョンを検証する。
- `run_one(graph, condition, seed, cancellation, registry) -> Result<RunResult>`: 保存先に依存しない単一実験。
- `run_batch(plan, runtime_options, cancellation, registry, event_sink) -> Result<BatchSummary>`: 保存・キャッシュ・並列実行を伴うアプリケーション処理。
- `RunResult`、`MeasurementRecord`、`RunTermination`: 一次データ中心の結果と終了状態。
- `RunView`: 条件・グラフ・結果から表示用の省略値と派生値を生成する読み取り層。
- `BatchSummary`: メモリ上の戻り値と標準出力専用の集計。自動保存しない。
- `VertexFitness`・`FitnessFactory`・`FitnessRegistry`: 適応度の実装と登録。検証・実行・再開には同じ登録情報を渡し、未登録やバージョン不一致を拒否する。
- Storeの読み取りAPIと`export_tsv(...)`: CLI以外からも保存結果を利用する入口。

バッチのevent_sinkにはジョブの終了状態を渡し、探索状態やRNGへの可変参照は渡さない。単一実験の計測値はRunResultから取得する。I/Oや表示の例外で計算結果を書き換えない。ソルバー・平滑化はジョブ所有の可変オブジェクトとし、共有Mutexで内部RNGを保護する設計を廃止する。

CLIは引数と表示、experimentは実験の進行、solversは探索規則、graph_partitionは問題固有の計算、storage/exportはI/Oを担当する。計算層からCLI・保存・TOMLパーサーへ依存しない。旧公開モジュール名への互換用再exportは設けない。

## 8. 既存コードの扱いと実装順序

### 並列実装と担当モデル

開発方針の正本は[AGENTS.md](../AGENTS.md)。親がAPI・所有ファイル・受入条件を確定し、独立作業を親+最大3サブエージェントで進める。同じファイルを複数担当へ割り当てない。

| 段階 | 並列分担 | 依存・統合条件 |
|---|---|---|
| 保全・文書 | 親が保全と基準測定、LunaがPlan・出力・開発文書 | 既存データを保全してから削除 |
| 契約・基盤 | Solが計算基盤、Terraが設定・展開、親が共有API | Graph・Condition・RunResultを合意 |
| 機能実装 | Solが探索・計測、TerraがCLI、親が保存・TSV | 合意したAPIだけで接続 |
| 独立検証 | 別のSolが参照計算・運用テスト、Lunaが利用文書 | 実装担当から独立して検証 |
| 統合 | 親が全体テスト・性能・文書整合、TerraがCI | 失敗を修正して受入基準を満たす |

Lunaは機械的更新、Terraは境界が明確な実装、Solは計算・乱数・再開等の複雑な処理に用いる。失敗の反復や範囲拡大時は上位モデルへ切り替える。モデル利用不可は代替し、未計測の費用削減額は主張しない。

### 段階1: 整理前の保全と基準測定

- 既存の未コミット変更を確認し、未追跡の実験ランナーと設定をGit管理外の`data/cleanup-backup/<timestamp>/`へ内容を変えずに保全する。
- 未完成ランナーをビルド対象から外し、既存回帰テスト80ケースと現行速度ベンチを実行して基準を記録する。
- GUI・SQA、依存と参照を削除し、同じ基準が維持されることを確認する。削除済み機能を復元する目的の未使用ソースを本番ツリーに残さない。

### 段階2: 計算基盤の一本化

- モジュールとPartitionStateを整理し、Flip/Swap・差分評価・HC/SA平滑化・各手法のステップ処理を実装する。EOのtau必須化、default適応度、適応度レジストリを導入し、k_averageを削除する。
- 全再計算する小規模参照実装との比較で各手法を確認し、コピー・全近傍生成を削減する。
- 乱数の分離、初期解の共通化、計測用の固定サンプル集合を導入する。この段階で計算仕様の変更理由を記録し、新仕様の基準を確立する。

### 段階3: 設定と実験計画

- TOML/JSON、型付き手法、全平滑化、スイープ、検証、メモリ上の計画展開と未展開条件の保存を実装する。
- 未完成ランナーから必要なスイープの意図だけを取り込み、旧API呼び出し・SQA・連続緩和・独自gnuplot処理は移植しない。
- 有効な例を`examples/configs`へ揃えた後、ルートの旧サンプルと旧実験バイナリを削除する。リポジトリ内に実行できないサンプルを残さない。

### 段階4: アプリケーションとしての実行管理

- 並列実行、グラフ共有、計測、最小限の結果保存、識別子、条件とファイルからの状態判定を統合する。
- キャンセル、部分結果、ジョブ単位の再開、書き込みロック、保存失敗と破損検出を実装する。
- 「保存成功=完了」という通知・状態更新の順序を確認する。

### 段階5: CLIと結果利用

- gppの全サブコマンド、進捗・JSON表示・終了コードを実装する。
- RunViewによる派生値補完とinspect・JSON/TSV出力を完成し、最小例と54ジョブ例を端から端まで実行する。
- 依存する不要コード、旧file_utils、旧run_executor、旧設定型を整理後のAPIへ完全に置き換える。

### 段階6: 品質確認と利用文書

- WindowsとLinuxでテスト・ビルド・保存・Ctrl+Cを検証するCIを追加する。
- READMEに導入、最短の実行例、再開、出力先、終了コードを載せる。詳細は本仕様と各分野の文書へリンクする。
- 本文書を実装に合わせて更新し、実装済み機能と一致した時点で提案状態を解除する。

## 9. 完了条件と検証

### 計算の正しさ

- 小規模グラフで全分割を列挙し、実スコア・差分評価・キャッシュ更新を照合する。最適値は比較用に使い、確率的手法に毎回の最適解到達を要求しない。
- HC・SA・EOについて、同じ乱数ストリームを与えた参照実装と各ステップの状態・最良値を照合する。
- 全平滑化、Kの境界、距離2の重複除去、alpha=0、次数0・n-1、温度0、同点処理を検証する。
- 現在解と最良解の区別、受理・棄却・走査数、初期・最終計測、ベイスン打ち切りの意味を検証する。

- Swapの等分割維持、交換頂点同士に辺がある場合の差分評価、距離1/2近傍の個数と重複除去を検証する。
- EOのdefault適応度・孤立頂点・順位抽選・Swapの条件付き抽選、配布CLIの登録がdefault一つであること、テスト用独自定義の注入から再開・出力までを検証する。

### 再現性と実験機能

- スレッド数1と複数、設定の並べ替え、表示名変更で結果が変わらない。
- 計測なし・実空間のみ・両空間、計測点の追加・削除、診断の有効・無効で探索の最終解と最良解が変わらない。共通の計測点の値も一致する。
- ステップ上限を延長した実行は、元の上限まで同じ探索軌跡を持つ。
- TOMLとJSONの等価設定は同じ解決済み計画と識別子になる。例の展開件数が54になる。
- 重複、未知キー、非有限値、負値、桁あふれ、無効な手法パラメータを実験開始前に拒否する。

- taus欠落・空配列・非正値、EOへのsmoothing、削除したk_average、未登録適応度、奇数頂点のSwapを拒否する。

### 運用と出力

- 成功・スキップ・グラフ失敗・結果保存失敗・中断が混在するバッチの件数、通知、終了コードが一致する。
- Ctrl+Cが近傍走査とベイスン計測にも届き、部分結果が完了扱いされない。
- 再開で完了結果の再計算を避け、残りの再実行結果が通常実行と一致する。
- 同一ルートへの二重起動、プロセス異常終了、書きかけJSON、破損キャッシュ、置換失敗、結果確定後・未完了マーカー削除前の終了から適切に復旧する。
- エクスポートの列、行数、条件識別、省略フィールドの補完と空欄、打ち切り・中断表示、タブや改行を含む名前を検証する。
- 展開済み計画・状態一覧・集計表・条件コピー・終点スコアを正本に保存せず、初期・終了点と共有分割参照を正しく復元する。途中の最良値を計測点の最小値で代用しない。
- 診断無効を0と誤表示しない。TSVを削除・再生成して実験の再実行なしに同じ値を得られる。
- 全コマンドのhelp、JSON表示、エラー時の終了コード、README記載の操作を実行テストする。

### 性能と完成判定

- fmt、clippy（警告をエラー化）、全ターゲットのテスト、releaseビルドが成功する。
- 既存SAの同条件で計測なしの探索速度を同一環境・同一ビルド設定で比較する。ウォームアップ後3回の中央値を用い、10%超の性能低下が再現する場合は原因を解消する。
- 新仕様の計測は内容と乱数が変わるため、旧計測との単純な時間比を合否基準にしない。ベンチでは診断を有効にして探索と計測の時間を分け、代表的なHC・SA・EOと距離2平滑化の結果をベンチ記録に残す。
- GUI・SQA・未完成実験バイナリがビルド対象と公開APIに存在せず、全サンプルが実行可能である。
- 設定作成→検証→計画→並列実行→中断→再開→確認→TSV出力を、補助スクリプトや手作業のJSON修復なしに完了できる。

この一連の完了条件を満たした状態を、本計画における「完成したアプリケーション」とする。
