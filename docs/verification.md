# 実装・検証記録（履歴）

ここは過去の実装・実測の証跡であり、現在のブランチまたはCIの成功を保証しない。現在必要な検証は`python scripts/check.py`で実行する。2026-09-15時点で完成版CLI・ライブラリと共有分割保存を実装した。以下は実際に確認した結果であり、未実行の検証を成功扱いしない。

## 完成までの実装履歴（2026-09-15）

この節は完了済みの実装段階と担当分割の履歴であり、現在の作業手順ではない。保全・文書、計算基盤、設定・計画、保存と実行管理、CLI・結果利用、品質確認の順で進めた。共有契約は親担当が確定し、計算・乱数・保存再開の複数モジュール変更はSol、設定・CLI・TSV・局所テストはTerra、機械的な文書・参照更新はLunaへ分担した。独立した参照計算・運用テストは実装担当と分離し、親が統合、性能測定、文書・schema整合を判定した。

この履歴で除外を確定した範囲はGUI、SQA、連続緩和、旧ランナー／旧公開API、`k_average`、追加の組み込みfitness、EOの別同点処理・抽選法、分散実行、サーバー・クラウド連携である。選択統合では暫定解からの実評価ベイスン、予算のスイープと条件別指定、ラウンド単位実行を現行APIへ取り込んだ。現行の仕様・受入基準は[application-plan.md](application-plan.md)、拡張時の手順は[extending.md](extending.md)を参照する。

## 保全と整理

- 既存のソース・テスト・使用例・Cargo設定・未追跡ランナーと設定を`data/cleanup-backup/20260915-100910/`へ保全した。既存実験データと作業開始前の`.gitignore`変更は維持している。
- 保全した旧実装の独立作業コピーで45単体テストと80ケースの回帰比較が成功した。その後、現行ツリーからGUI、SQA、旧バイナリ、旧API、k_average、不要依存を除去した。
- 全体Plan・出力仕様・AGENTS.md・README・アルゴリズム説明を新実装に合わせた。

## Windowsでの検証

- `cargo fmt --all -- --check`
- `cargo clippy --all-targets -- -D warnings`
- `cargo test --all-targets`: 30テスト成功（単体13、実験受入12、CLI2、独立参照3）。
- releaseビルドとライブラリ例。
- 比較設定は12頂点、最大100ステップ、近傍2種類、SA6設定・HC1設定・EO2設定、3シード。54ジョブすべて成功した。
- 同じバッチのresumeでは54ジョブを再利用し、再計算しなかった。
- exportは54実行行・989計測行を生成した。再生成した2つのTSVのSHA-256が一致した。
- 結果JSONの解参照が有効で、current_real/best_real、分割配列・条件・シードの重複フィールド、manifest等を保存していないことを確認した。
- 型の参照範囲・分割長・Swap等分割・暫定解の単調性・最終参照一致・計測点・診断適用の検証、上書き失敗時の旧結果保全、部分結果export、Windowsロック競合をテストした。
- 計測点間の改善保持、計測頻度・診断・並列数に依存しない探索、予算延長の共通軌跡、カスタム適応度、距離1/2の独立全列挙平均を確認した。

## 探索性能

同一Windows環境・release設定、Random 500頂点・期待次数5・グラフシード42、Flip、SA温度1、100万ステップ、平滑化なしで測定した。ウォームアップ1回の後、3回の中央値を比較する。

旧実装は保全したコピーからベイスン計測呼び出しだけを外して探索を測定し、48.35msだった。新実装は`cargo run --release --example bench_search`で初期・終了点だけを保存し、ベイスンなしで測定した。最終ビルドの再測定は32.428、45.093、53.362ms、中央値45.093msで、10%超の性能低下は認めなかった。乱数導出仕様は変更しており、同一探索軌跡の速度比較ではない。

測定値はこのマシンの一条件の結果であり、全手法・全グラフでの速度保証ではない。平滑化・ベイスン計測の時間は探索速度と分けて扱う。

## 環境上の未確認事項

WindowsとUbuntuのCI定義を追加した。ローカルではWSLに通常のLinux開発環境がなく、Dockerエンジンも時間制限付き確認で応答しなかったため、Linux側のビルド・テストは実行できていない。GitHub ActionsのLinuxジョブ成功確認が残る。

協調キャンセルトークンと部分結果はテスト済み。実コンソールへのCtrl+C送信は自動検証していない。CLIには1回目でキャンセル、2回目で終了コード130のハンドラーを実装している。

## 計算結果を維持する高速化（2026-09-15）

`51577f9` を基準にEO、非平滑化SA/HC、実評価ベイスンを高速化した。変更内容、完全一致の範囲、測定条件と結果は[performance.md](performance.md)を参照。

- Windowsでfmt確認、全ターゲットclippy（警告をエラー扱い）、全ターゲットテスト37件、releaseの完全一致テスト7件、release CLIビルドが成功。
- Engine/runnerの固定参照実装と、旧適応度計算に対する比較を追加。全ステップの分割・評価値のビット・乱数系列・診断カウンターと、保存結果の分割登録順・参照・ベイスン値を照合。
- 64条件の主測定、32条件の長時間SA、16条件のHC、16条件のベイスンで、時間・試行IDを除く結果署名がすべて一致。
- Windows/Ubuntu CIにreleaseの完全一致テストを追加。今回もローカルのLinux検証は未実行で、CIの実行結果は未確認。

## masterの3機能を選択して統合（2026-09-15）

統合前の現行ブランチは`afb153d`、取り込むmasterの履歴は`c0dd61f`。暫定解からの実評価ベイスン、計算予算スイープ・条件別指定、ラウンド単位実行の3機能を現行APIへ移植した。マージは現行ツリーを基礎に選択した機能を加える方式で、masterにある旧探索実装や削除済み機能は復活させない。Terraが設定・展開、Solが暫定解計測とラウンド・保存を分担し、親が契約・文書・統合検証を担当した。計測担当によるラウンド保存の独立レビューも実施した。

- Windowsで`cargo fmt --all -- --check`、`cargo clippy --all-targets --all-features --locked -- -D warnings`、`cargo test --all-targets --locked`が成功。全62件（単体22、既存実験12、暫定解ベイスン7、予算7、CLI3、独立参照3、ラウンド8）。
- `cargo test --release --lib exact_tests --locked`の固定参照との完全一致7件が成功。既存の探索・計測値、分割参照、診断カウンター・RNG系列の回帰を確認した。
- 暫定解ベイスンの有無で探索軌跡が同じ。同一暫定解が続く場合、6頂点の孤立グラフでは密な計測でも実評価回数は初回の7回だけだった。計測頻度を変えても同じ暫定解の計測値が一致する。
- 暫定解ベイスンのキャンセル時に未完成キャッシュを登録しないこと、完全4頂点グラフの全列挙最適値との一致、欠落・悪化値・不正な平滑化値や走査数の拒否を検証した。
- 予算のスカラー・配列、1要素配列の正規化、TOML/JSON等価性、グループ継承・上書き、空・0・重複・ルート指定との混在・予算を超える計測点の拒否を確認した。
- ラウンドのシード昇順、途中で期限到達しても当該ラウンドは完了すること、次ラウンドの未開始、再開時の完了結果再利用、失敗時の後続ラウンド停止、キャンセルの優先を検証した。通常実行とラウンド実行の結果は時間・試行IDを除いて一致する。
- 24ジョブ設定`examples/configs/rounds.toml`のCLIで、期限0による停止、resumeで24件完了、再度のresumeで24件再利用、inspect、TSV出力まで成功。3ラウンドを完了し、予算と暫定解ベイスン列・メタデータを確認した。
- 統合前`afb153d`のrelease CLIで生成した比較例54ジョブを、新CLIのラウンドresumeが54件すべて再利用した。バッチIDは`e9bb99e4b662c118c11994b98e5d6a192843c112e39dae07521850beb0fb089a`を維持する。
- 出力仕様のJSON例を結果バリデーターへ通し、同じ分割から生成した実評価値と文書の表が一致することをテストした。
- `cargo build --release --all-targets --locked`が成功。release CLIで24ジョブ例を未展開の固定条件JSONへ保存し、そのJSONから3ラウンド・24件を完走した。exportは24実行行・96計測行を生成し、再生成した両TSVのSHA-256が一致した。

既存の実験データは変更せず、手元の`.gitignore`変更はマージ用変更と分離して保全する。Linux実行と実コンソールCtrl+Cの未確認事項は上記のとおり継続する。

## 継続開発の監査に対する修正（2026-09-15）

監査基準は`2aae96a`。グラフの不変性、保存復旧のエラー分類、RunViewとTSV列定義の集約、拡張・版管理の契約、文書の役割分離、開発とCIの検証入口を修正した。計算・保存の確認は実装担当と分離し、出力は凍結した旧実装との比較で確認した。

- `Graph`の頂点数・辺を非公開とし、読み取り用の`node_count()`・`edges()`を提供した。Rust APIの破壊的変更を示すためパッケージ版は`0.2.0`へ更新した。計算・乱数・保存schema・条件IDの版は変更していない。依存パッケージも更新していない。
- Windows、`rustc 1.97.0 (2d8144b78 2026-07-07)`、Python 3.13.7で`python scripts/check.py`が成功。fmt、全ターゲット・全機能のclippy（警告をエラー扱い）、全ターゲット75テスト、Rustdocのcompile-fail 2テスト、release完全一致7テスト、release全ターゲットビルド、警告をエラー扱いしたAPI文書生成を実行した。
- 保存復旧8テストで、不正・将来版のマーカー、完了結果の保全、破損バックアップ、実I/Oエラーの伝播、グラフ準備失敗の記録を確認した。カスタム適応度の実行例は、登録・条件検証・保存・読み込み・再開と不正パラメータ拒否をテストした。
- 凍結した`2aae96a`のexportと128条件を比較し、完了結果、計測のない中断終了点、診断・ベイスンの有無を含めて全TSVバイトとmetadataの列名・型・単位が一致した。列説明文は改善を許容している。
- 変更前のrelease CLIで保存した128件を、新しいCLIのラウンドresumeが再計算せずすべて再利用した。同じバッチIDを維持し、再生成した128実行行・357計測行の両TSVは変更前とSHA-256が一致した。比較用データは`target/`内に作成し、既存の実験データは変更していない。
- CIのWindows/Ubuntuジョブも同じ検証スクリプトを呼ぶ構成にした。今回のLinux実行およびGitHub Actionsの成功は未確認。性能測定は実施しておらず、この修正による速度改善は主張しない。

## 2026-09-25 の変更

親エージェントが確定した契約に基づき、EOへ組み込み適応度`multiplicative`（`multiplicative-v1`、params`alpha`）と`additive`（`additive-v1`、params`beta`）を追加した。2026-09-15時点の完成履歴（上記）が除外した「追加の組み込みfitness」は、multiplicative・additiveに限りユーザー判断で復活させた。同時に、同じく除外していた「EOの別同点処理・抽選法」も見直し、EOの選択規則をアルゴリズムv2へ変更した。毎ステップのシャッフル+安定ソートによる同点処理を廃止し、標準順序（λ昇順・side・頂点id）上の同点ブロックへ重みを平均化して割り当てる規則（旧`select_eo_rank`の規則）を採用した。`tie_rng`はEOで使わなくなった（HCは従来どおり使用する）。組み込み適応度は差分更新索引でこの選択規則を実行し、索引を持たないカスタム適応度は毎ステップ全頂点を評価する汎用経路を使う。

EOの`tau`は`0`以上（`tau >= 0`）を受け付けるよう変更し、`tau > 0`必須の制約を撤廃した。`pinned_versions`の`algorithm`は`v1`から`v2`へ変わり、全条件IDが変わる（SA・HCの探索軌跡はビット同一のまま、条件IDだけが変わる）。

保存結果JSONの`partitions`は、`Vec<Vec<bool>>`のJSON表現を`{ "length": n, "hex": [...] }`という、頂点をビット詰めした小文字16進文字列の配列へ変更した（`schema_version`は1のまま）。`seed_<seed>.json`と`seed_<seed>.incomplete.json`は整形なしの1行JSON（+末尾改行）で書き込むようにした。`experiment.json`とグラフJSONの整形は変更していない。

`serde_json`へ`float_roundtrip`機能を追加した。従来は保存JSONから読み戻した`f64`が1 ULPずれることがあり、`experiment.json`から再展開した条件IDが元のTOMLと食い違っていた（`10^(k/1000)`の温度624点を含む661条件で74条件の条件IDとバッチIDが不一致）。追加後は同じ確認で不一致0件となり、`Cargo.lock`は変わらない。

検証（Windows、2026-09-25）:

- `python scripts/check.py` がすべて成功した（fmt、locked clippy `-D warnings`、全ターゲット／docテスト、release exact regression、release build、警告をエラーにするdoc生成）。
- EO v2 は独立に書いた素朴な参照実装（`src/solvers/test_reference/eo_v2_reference.rs`）と、組み込み7設定×Flip/Swap×tau 0〜1e308×複数シードの全ステップで、分割・`select_rng`の状態・評価値・診断カウンターがビット一致した。u の全格子（2^53）上の選択閾値も参照実装と一致し、演算を1 ulp だけ動かす変更も検出する。SA・HC・ベイスンは凍結した`51577f9`との比較を維持し、すべて一致した。
- 実装担当とは別のエージェントが EO と保存形式をそれぞれレビューし、どちらも阻害要因なし。EO レビューは全2^53値の厳密確率と閉形式の一致、χ²検定、索引と再構築の一致、変異テスト（30件中27件検出。残る3件のうち2件は追加した閾値テストとEOキャンセルテストで検出し、1件は観測可能な差を生まない等価変異）を確認した。旧v1 との分布の違い（Flip は1ステップの分布が同一、Swap の2頂点目は異なり v2 は旧実装 `c0dd61f` と同じ）は [algorithms.md](algorithms.md) の「EOの選択規則v2」に記した。
- 速度（release、n=500 ランダム、1e6 ステップの1ステップあたり時間、単独実行）: 3回の中央値で Flip default 10.43→0.43µs、Flip multiplicative 0.5 12.91→0.82µs、Swap default 18.14→0.71µs（期待次数5。変更前の実装に新しい適応度モジュールを入れて比較）。期待次数20では5〜8.5倍。12並列の10^7ステップ1 run は Flip default が52〜164秒から9〜12秒、Swap default が87〜282秒から19〜34秒（n=124〜500）。
- 保存容量: 1 run（n=500、Flip SA、1e6 ステップ、対数計測56点、basin real + best_basin）が422,741→18,143バイト、n=124 は113,930→11,857バイト。10^7ステップでは n=124 で約15KB、n=500 で約22KB。

## 2026-09-26 の変更

長谷川「Extremal Optimization法とアニーリング法の比較評価に関する再検討」（第14回最適化シンポジウム、2022）の混合アルゴリズムを、Flip/Swapへ移したEO-SA（`eo_sa`）を追加した。平滑化は利用者の指示で扱わない。規則の正本は[algorithms.md](algorithms.md)の「EO-SA（EOの提案とMetropolis判定）」である。既存手法のシリアライズと`pinned_versions`は変えていないので、既存の条件IDは変わらない。

分担は[AGENTS.md](../AGENTS.md)に従った。親エージェントが契約（設定型と規則）、本体と周辺の実装、統合を担当した。独立した参照実装とビット一致テスト、統合テストと設定例、文書は、それぞれ別のエージェントが担当した。GPT-5.6の各モデルは使えなかったので、Claudeのモデルで代替した（Sol相当はOpus、Terra相当はSonnet、Luna相当はHaiku）。

現在のstable（Rust 1.98）のclippyが既存コードに新しいlint（`manual_isolate_lowest_one`、`chunks_exact_to_as_chunks`）を出し、masterのCI（2026-09-25のrun）が失敗していた。動作が同じ置き換えで対応した。

検証（Linux、rustc 1.98.1、2026-09-26）:

- `python scripts/check.py`がすべて成功した。
- EO-SAは、独立に書いた素朴な参照実装（`src/solvers/test_reference/eo_sa_reference.rs`。選択部分はEO v2の参照実装を使う）と全ステップでビット一致した。
  - 条件：組み込み7設定×4グラフ×Flip/Swap×tau 0〜1e308×温度{0, 0.05, 0.5, 2, 1e300}と、独自適応度の汎用経路。
  - 照合対象：分割、`select_rng`と`accept_rng`の状態、評価値のビット、適用移動数、評価回数、適応度の計算回数、索引の整合。
  - 改善・同点・上りの受理・上りの棄却・温度0での乱数なしの棄却の各分岐を通ることもテストで確かめた。参照実装を4通りに変異させると、いずれも検出された。
- 温度1e300で同じ選択系列を与えるとEOと同じ軌跡になること、温度0で受理用の乱数を消費しないこと、中断したステップの乱数と計数、runnerの暫定解と診断値を確かめた。
- 設定の検証・展開・正規化、条件IDの独立性、実行・再開・export、独自適応度の登録を、統合テスト（`tests/eo_sa.rs`、18件）で確かめた。ベースラインA・復旧B/Cの`batch_id`は記録と一致した。
- 独自適応度では、中断したステップでも選択のために計算したn回の適応度が計算回数に入る（EOと同じ）。当初の文書と食い違っていたので、文書をこの挙動に合わせた。
- 速度（release、n=500のRandom・期待次数10、10^6ステップ、1スレッド、計測なし、各1回）。他の作業と同時に測った目安である。1ステップあたりの時間は次のとおり。
  - Flip：SA 0.029µs、EO（τ=1.5）0.80µs。EO-SA（τ=1.5）はT=0.3で0.15µs（受理率34%）、T=1で0.42µs（60%）、T=1e300で0.82µs（100%）。
  - Swap：SA 0.075µs、EO 1.77µs。EO-SAはT=0.3で0.63µs（53%）、T=1で1.03µs（62%）、T=1e300で1.77µs（100%）。
  - 棄却した手では順位を更新しないので、受理率が低いほど速い。
